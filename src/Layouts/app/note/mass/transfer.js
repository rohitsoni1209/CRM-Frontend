import React from "react";
import MassTransferList from "../../../../Components/massTransferList";

const MassTransferNote = () => {
  return <MassTransferList key="Note" moduleName="Note" />;
};

export default MassTransferNote;
